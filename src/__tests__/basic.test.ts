import { describe, it, expect } from 'vitest';

// Simple test to verify our application structure
describe('Git Author Analyzer', () => {
  it('should have basic functionality', () => {
    // This is a placeholder test
    expect(1).toBe(1);
  });
});